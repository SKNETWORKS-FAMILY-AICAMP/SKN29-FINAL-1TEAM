"""Agent Context — 에이전트 프롬프트에 실을 **카탈로그 섹션**의 조립처.

## 왜 있나

LLM 에이전트가 룰을 쓰거나 내규를 검증하려면 우리 도메인의 어휘를 알아야 한다 —
DSL 연산자, EvalContext 허용 경로, 판정 선택지, 플래그 코드, 별표 축. 이 값들은 전부
모델·enum에 실재하는데, 지금까지는 프롬프트에 **일부만**, 그것도 에이전트마다 제각각
하드코딩된 사본으로 들어갔다(`agent.py`의 `_ALLOWED_OPS`, `django_client`의 폴백,
프롬프트 안의 `EvalContext v4` 리터럴 — 코드는 이미 v5였다).

이 패키지는 그 카탈로그를 **한 곳에서 live 모델로부터** 만든다.

## 경계 — core는 사실, ai는 문장

여기서 나가는 건 **구조화 JSON**이다. 프롬프트 문구·토큰 예산·머리말 서식은 ai가
(`app/context/render.py`) 정한다. 프롬프트 표현을 바꾸려고 Django를 재배포해야 하면
아무도 프롬프트를 안 고친다.

다만 **불변식 한 줄**(`notes`)은 core가 소유한다 — "플래그는 상태머신을 움직이지
않는다" 같은 문장은 표현이 아니라 도메인 규칙이고, 어기면 ai가 아니라 여기가 깨진다.

## 불변식 — 프롬프트와 검증기는 같은 카탈로그를 본다

`build()`가 만든 카탈로그가 프롬프트로 나가고, **같은 값**이 `_sanitize_nodes`·
`validate_graph_vars`·`unknown_flags`의 판정 기준이 된다. 둘이 갈리는 순간 이 패키지의
존재 이유가 없어진다("모델에게 말한 것"과 "우리가 강제하는 것"이 다른 상태로 돌아간다).
"""
