# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Claude / personal (repo 관례 유지)
.personal/
.claude/
CLAUDE.local.md
.mcp.json

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Python / Django (apps/core, apps/ai)
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Django
apps/core/media/
apps/core/staticfiles/
*.sqlite3
*.log

# 컨테이너 로그 바인드 마운트(compose `./logs:/logs`) — core.log / ai.log + 로테이션 백업.
# 디렉터리째 무시한다: `*.log`만으로는 `core.log.1` 같은 백업 파일이 안 걸린다.
/logs/

# FastAPI(ai) — 로컬 모델/벡터 산출물
apps/ai/var/
#  시드가 남기는 골든 라벨·채점 산출물 — 데이터이지 코드가 아니다(재현은 시드로).
apps/core/var/
*.pkl
*.joblib
# Chroma 로컬 영속 클라이언트 산출물(docker 없이 돌릴 때). 벡터 DB는 커밋하지 않는다.
chroma_data*/

# Node / React (apps/web)
node_modules/
apps/web/dist/
apps/web/.vite/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Docker / data volumes (로컬 바인드 시)
.data/
*.pid
apps/web/tsconfig.tsbuildinfo

# ml/ CatBoost training-diagnostics dump (regenerated on each run, not the saved model)
ml/catboost_info/