# ml/ — 법인카드 이상거래 탐지 작업 전체 안내

이 폴더에 뭐가 있는지 한눈에 찾기 위한 최상위 안내. 각 하위 폴더에 더 자세한 README가 있다.

| 폴더/파일 | 내용 | 상태 |
|---|---|---|
| `ML_0728/` | **팀원 원본** — Isolation Forest(MVP 후보) 전처리·모델링 노트북 4개 + 결과 요약 md | 커밋됨(git log: `ML isolation forest`) |
| `pipeline/` | 지도학습 8개 모델 3라운드 재현 스크립트+노트북, 피처 로더 | 미push (이번 세션 작업) |
| `models/` | `pipeline/` 스크립트가 만든 산출물(모델 pkl, 결과 csv/json, 로그) — 라운드별 하위 폴더 | 미push |
| `법인카드_이상거래_EDA*.ipynb` (3개) | 초기 EDA 노트북 | 커밋됨 |
| `법인카드_이상거래_전처리.ipynb` | 전처리 노트북 | 커밋됨 |
| `전처리_노트북_리뷰_통합본.md` | 전처리 리뷰 정리 | 커밋됨 |

## 모델링 결과를 보고 싶다면

`models/README.md`를 먼저 열어볼 것 — 3라운드 실행 결과, 현재 MVP 방향(재검토 중), 지도학습
vs 비지도 비교가 전부 정리돼 있다.

## 코드를 재현/재실행하고 싶다면

`pipeline/`의 스크립트를 그 폴더 안에서 실행한다(예: `cd ml/pipeline && python model_training.py`).
Isolation Forest MVP는 이 폴더에 재현 스크립트가 없다 — `ML_0728/`의 팀원 노트북이 기준이다.
