# archive/ — 참고용 보관 (현재 배포 대상 아님)

최종 확정된 MVP([`../mvp_isolation_forest/`](../mvp_isolation_forest/))가 아닌, 그 결정에 이르기까지
거쳐온 실험·초기 작업을 보관하는 폴더. 지우지 않고 남겨두는 이유는 "왜 이 방향으로 확정됐는지"의
근거(비교 실험 수치)를 나중에도 추적할 수 있게 하기 위함이다.

| 폴더 | 내용 |
|---|---|
| [`supervised_experiments/`](./supervised_experiments/) | 지도학습 8개 모델(XGBoost·LightGBM 등) 비교 실험 — 참고 자료, 배포 대상 아님 |
| [`early_eda_preprocessing/`](./early_eda_preprocessing/) | 초기 EDA·전처리 노트북 — `mvp_isolation_forest/`가 최신 기준 |
